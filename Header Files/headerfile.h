// This line makes sure that this file is only included once
// no matter how many times it's "included"
#pragma once

// These are just the function definitions.
// Note that there's no code / logic in these functions...just the definitions
namespace MathFunctions {
    void PrintAddingInts();

    int Add(int a, int b);
}

//Test
